%% generateStudents: Generate Student array
%
% generateStudents takes in a path to the autograder's working folder, and
% converts the student data to a Student array.
%
% [Student[] students] = generateStudents(string path);
%
%%% Remarks
%
% (describe the types of inputs and outputs)
% input will be the full path to the student's folder (always a string or 
% char?)
% output will be student array?
%
%%% Exceptions
%
% ?
%
%%% Unit Tests
% ideas: 1) path is valid and folder is there correctly
%        2) if directory doesn't exist
%        3) if path is invalid
%
% format:
%    inputs
%    function call
%    
%    output